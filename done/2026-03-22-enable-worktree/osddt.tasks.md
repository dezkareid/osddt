# Tasks: Enable Worktree Workflow

## Phase 1 — CLI: `start-worktree` command

- [x] [M] Create `src/commands/start-worktree.ts` — resolve repo root via `git rev-parse --show-toplevel`
- [x] [S] Read `worktreeBase` from `.osddtrc`; fall back to `<repo-root-parent>/<repo-name>-<feature-name>`
- [x] [M] Check branch exists locally and remotely; offer Resume / Abort if found
- [x] [M] Run `git worktree add` (new branch `-b` or existing branch)
- [x] [S] Resolve project path from `.osddtrc` (`single` → worktree root; `monorepo` → prompt for package)
- [x] [S] Create `<project-path>/working-on/<feature-name>/` inside the worktree
- [x] [M] Read/write `.osddt-worktrees` at `<repo-root-parent>/.osddt-worktrees`; append `{ featureName, branch, worktreePath, workingDir, repoRoot }`
- [x] [S] Print summary: branch, worktree path, working directory
- [x] [S] Register `start-worktree` command in `src/index.ts`

**Definition of Done**: `osddt start-worktree <feature>` creates a worktree, scaffolds `working-on/`, and writes the state file entry.

---

## Phase 2 — CLI: `worktree-info` command

> Depends on: Phase 1 (state file format defined)

- [x] [S] Create `src/commands/worktree-info.ts` — accept `<feature-name>` argument
- [x] [S] Read `.osddt-worktrees`; locate entry by `featureName`
- [x] [S] Print JSON `{ worktreePath, workingDir, branch }` on success; exit code 1 with message if not found
- [x] [S] Register `worktree-info` command in `src/index.ts`

**Definition of Done**: `osddt worktree-info <feature>` returns correct JSON when entry exists and exits with code 1 when not found.

---

## Phase 3 — CLI: update `done` command

> Depends on: Phase 1 (state file format), Phase 2 (`worktree-info` for path resolution)

- [x] [S] Add `--worktree` flag to `done` command in `src/commands/done.ts`
- [x] [M] When `--worktree`: read `.osddt-worktrees`, find entry, run existing move logic, then `git worktree remove <worktreePath> --force`
- [x] [S] Guard against double-run: skip `git worktree remove` if path no longer exists on filesystem
- [x] [S] Remove entry from `.osddt-worktrees` and write back after cleanup
- [x] [S] Without `--worktree`: behaviour unchanged

**Definition of Done**: `osddt done <feature> --worktree` archives the planning folder, removes the worktree, and cleans the state file. `osddt done <feature>` (no flag) behaves exactly as before.

---

## Phase 4 — Template: `osddt.start-worktree`

> Depends on: Phase 1 (`start-worktree` CLI command available)

- [x] [M] Add `osddt.start-worktree` entry to `COMMAND_DEFINITIONS` in `src/templates/shared.ts`
- [x] [S] Template reads `.osddtrc`, runs `npx osddt meta-info`, derives branch/feature name (same rules as `osddt.start`)
- [x] [S] Template calls `npx osddt start-worktree <feature-name> [--dir <package-path>]` and reports branch, worktree path, working directory
- [x] [S] Template next step points to `/osddt.spec`

**Definition of Done**: `osddt.start-worktree` template is generated by `osddt setup` and produces correct Markdown output with the right CLI call.

---

## Phase 5 — Template: update `osddt.continue`

> Depends on: Phase 2 (`worktree-info` CLI command available)

- [x] [M] Update `osddt.continue` body in `shared.ts`: before phase-detection, instruct agent to run `npx osddt worktree-info <feature-name>`
- [x] [S] If exit 0: use returned `workingDir` as the working directory
- [x] [S] If exit 1: fall back to existing main-tree scan logic

**Definition of Done**: `osddt.continue` template correctly resolves working directory for both worktree and non-worktree features.

---

## Phase 6 — Template: update `osddt.done`

> Depends on: Phase 2 (`worktree-info`), Phase 3 (`done --worktree`)

- [x] [M] Update `osddt.done` body in `shared.ts`: instruct agent to run `npx osddt worktree-info <feature-name>` first
- [x] [S] If not found: call `npx osddt done <feature-name> --dir <project-path>` (existing behaviour)
- [x] [M] If found (worktree feature): instruct agent to run `git -C <worktreePath> status --porcelain` to check for uncommitted changes
- [x] [M] If changes exist: instruct agent to run `git diff`, derive a conventional commit message, present it to the user for confirmation or override, then commit with `git add -A && git commit -m "<message>"`
- [x] [S] Push the branch: `git push --set-upstream origin <branch>` (covers both first push and subsequent pushes)
- [x] [S] After commit + push: call `npx osddt done <feature-name> --dir <project-path> --worktree`
- [x] [M] Update `shared.spec.ts`: assert `osddt.done` body references `git status`, `git diff`, `git push`, and commit message prompt

**Definition of Done**: `osddt.done` template handles both worktree and non-worktree features without regression.

---

## Phase 7 — CLI: `setup-worktree` command

- [x] [S] Create `src/commands/setup-worktree.ts`
- [x] [S] Check git version `>= 2.5` via `git --version`
- [x] [S] Confirm current repo is not itself a worktree via `git rev-parse --git-common-dir`
- [x] [S] Check sibling directory (or `worktreeBase`) is writable
- [x] [S] Print pass/fail summary per check; exit with code 1 if any check fails
- [x] [S] Register `setup-worktree` command in `src/index.ts`

**Definition of Done**: `osddt setup-worktree` reports environment readiness and exits non-zero on failure.

---

## Phase 8 — Documentation

> Depends on: Phases 1–7 complete (final command names and behaviour confirmed)

- [x] [S] Add `osddt.start-worktree` to the Command Templates table in `CLAUDE.md` and `AGENTS.md`
- [x] [S] Add `start-worktree`, `worktree-info`, `setup-worktree` to the CLI Commands table in `CLAUDE.md` and `AGENTS.md`
- [x] [S] Document `.osddt-worktrees` state file format and location in `CLAUDE.md`, `AGENTS.md`, and `README.md`
- [x] [S] Document optional `worktreeBase` field in `.osddtrc` in `CLAUDE.md`, `AGENTS.md`, and `README.md`
- [x] [M] Add **Example D — Parallel feature workflow** to `README.md` and `CLAUDE.md`
- [x] [M] Add "Parallel Development" section to `README.md` explaining the worktree workflow and when to use it

**Definition of Done**: All three documentation files accurately reflect the new commands, state file, and workflow with a clear example.

---

## Phase 9 — Tests

> Depends on: Phases 1–6 complete

- [x] [L] Write `src/commands/start-worktree.spec.ts`: mock `fs-extra` and `child_process`; assert state file written, working dir created, branch created, Resume/Abort flow
- [x] [S] Write `src/commands/worktree-info.spec.ts`: assert correct JSON output when entry exists; assert exit 1 when not found
- [x] [S] Write `src/commands/setup-worktree.spec.ts`: assert pass/fail output per environment check
- [x] [M] Extend `src/commands/done.spec.ts`: assert `git worktree remove` called with `--worktree` flag; assert state entry removed; assert double-run guard
- [x] [M] Extend `src/templates/shared.spec.ts`: assert `osddt.start-worktree` present in `COMMAND_DEFINITIONS`; assert `osddt.continue` and `osddt.done` bodies reference `worktree-info`
- [x] [S] Run full test suite (`pnpm test`) and confirm all tests pass

**Definition of Done**: All new and existing tests pass with no regressions.

---

## Dependencies Summary

```
Phase 1 (start-worktree CLI)
  └── Phase 2 (worktree-info CLI)
  └── Phase 4 (template: start-worktree)
        └── Phase 1
  └── Phase 3 (done --worktree)
        ├── Phase 1
        └── Phase 2
              └── Phase 5 (template: continue)
              └── Phase 6 (template: done)
Phase 7 (setup-worktree CLI)  ← independent
Phase 8 (docs)                ← after all phases
Phase 9 (tests)               ← after phases 1–6
```
